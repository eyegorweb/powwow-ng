# Included scalars
scalar JSON

type AvailableFilter {
  name: String!
  title: String!
  type: String!
  values: [Choice]
}

type Choice {
  code: String!
  label: String!
}

type Query {
  availableFilters: [AvailableFilter]
}
